# AGENTS

All agent rules for this repository live in [`CLAUDE.md`](./CLAUDE.md).

**Rule:** read `CLAUDE.md` and follow it. It is the single source of truth for
project setup, git and deploy workflow, database guardrails, planning workflow,
working rhythm, reliability requirements, and code conventions.

Do not add rules to this file. If a rule needs to change, change it in
`CLAUDE.md` so every agent sees the same instructions.
